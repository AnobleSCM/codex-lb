- [x] Add optional `refresh_repo_factory` parameter to `AuthManager.__init__` (`app/modules/accounts/auth_manager.py`), typed `Callable[[], AbstractAsyncContextManager[AccountsRepositoryPort]] | None`, stored on the instance with an explanatory comment tying it to the detached-task session-leak.
- [x] Add `AuthManager._run_refresh(account)` as the singleflight body and point `ensure_fresh`'s singleflight factory at it. When `refresh_repo_factory` is set, open a fresh repo (`async with self._refresh_repo_factory() as repo`) and run `refresh_account` via a temporary `AuthManager(repo, acquire_refresh_admission=self._acquire_refresh_admission)`; otherwise fall back to `self.refresh_account(account)`.
- [x] Add `ProxyService._accounts_refresh_scope` (`@asynccontextmanager`) yielding `repos.accounts` from the existing `self._repo_factory()`, and pass `refresh_repo_factory=self._accounts_refresh_scope` where `_ensure_fresh` constructs `AuthManager`. Add the `from contextlib import asynccontextmanager` import and extend the auth_manager import to include `AccountsRepositoryPort`.
- [x] Add `test_ensure_fresh_detached_refresh_owns_session_on_caller_cancel` to `tests/unit/test_auth_manager.py`: refresh in-flight → cancel the caller (simulated client disconnect) → assert the refresh wrote via the owned session (`owned_repo.tokens_payload` set, account id correct), never the request-scoped repo (`request_repo.tokens_payload is None`), and that the owned scope was opened and closed.
- [x] Prove the regression test has teeth: temporarily neuter `_run_refresh` to always use the bound repo and confirm the new test fails (`assert owned_repo.tokens_payload is not None` → None), then restore the fix.
- [x] `uv run pytest tests/unit/test_auth_manager.py -q` green (9 passed).
- [x] `uv run ruff check app/modules/accounts/auth_manager.py app/modules/proxy/service.py tests/unit/test_auth_manager.py` (All checks passed) and `uv run ruff format --check` on the touched files (already formatted).
- [x] `uv run ty check app/modules/accounts/auth_manager.py app/modules/proxy/service.py` (All checks passed) and import sanity (`import app.modules.proxy.service` OK).
- [x] Add `ADDED Requirements` delta to `openspec/changes/fix-auth-refresh-detached-session-leak/specs/database-backends/spec.md` codifying detached/shielded tasks own their session lifetime.
- [ ] Run `openspec validate --specs` where the OpenSpec CLI is available (structural review only if the CLI is absent on the runtime).
- [ ] Deploy: build `codex-lb:local-<sha>` via `scripts/codex-lb-deploy.sh`, verify alembic parity, retag `codex-lb:active`, recreate the container, confirm `/health=200` and no `QueuePool limit` recurrence. (Gated on Andrew's go; the watchdog covers recurrence meanwhile.)
