- [x] Add `RATE_LIMITED_COOLDOWN_SECONDS` constant near `QUOTA_EXCEEDED_COOLDOWN_SECONDS` in `app/core/balancer/logic.py`. Re-export it from `app/core/balancer/__init__.py` so `app/modules/proxy/load_balancer.py` can import it alongside the existing cooldown constant.
- [x] Update `_state_from_account` in `app/modules/proxy/load_balancer.py` so the `cooldown_ready` derivation for `account.status == AccountStatus.RATE_LIMITED` accepts EITHER the existing in-memory path (`runtime.cooldown_until` set and elapsed AND `runtime.blocked_at` not None) OR a DB-derived fallback (`effective_blocked_at` not None AND `time.time() >= effective_blocked_at + RATE_LIMITED_COOLDOWN_SECONDS`).
- [x] Rewrite the comment block at `app/modules/proxy/load_balancer.py:1062-1067` so it reflects that `rate_limited` now also recovers from a persisted `blocked_at` marker. Drop the "runtime-only" wording.
- [x] Add a regression in `tests/unit/test_load_balancer.py::test_state_from_account_clears_rate_limited_after_restart_with_persisted_blocked_at` mirroring the QUOTA_EXCEEDED variant at line 530: `status=rate_limited`, `reset_at` in the future, persisted `blocked_at` older than `RATE_LIMITED_COOLDOWN_SECONDS`, fresh `primary_entry` with `used_percent < 100` recorded after the block → `state.status == AccountStatus.ACTIVE`.
- [x] Add a regression `test_state_from_account_keeps_rate_limited_after_restart_when_persisted_blocked_at_is_recent` covering the inverse: `blocked_at` younger than `RATE_LIMITED_COOLDOWN_SECONDS` → `state.status == AccountStatus.RATE_LIMITED`.
- [x] Add a regression `test_state_from_account_keeps_rate_limited_after_restart_when_primary_usage_is_older_than_block` covering the freshness debounce: `primary_entry.recorded_at` <= `effective_blocked_at` → `state.status == AccountStatus.RATE_LIMITED`.
- [x] Add a regression `test_state_from_account_keeps_rate_limited_after_restart_when_primary_still_full` covering the false-recovery guard: persisted `blocked_at` old enough AND fresh `primary_entry` but `used_percent >= 100` → `state.status == AccountStatus.RATE_LIMITED`.
- [x] Add a regression `test_state_from_account_keeps_rate_limited_during_active_runtime_cooldown_even_when_db_threshold_elapsed` proving the DB-derived fallback does NOT shorten a still-active in-memory `runtime.cooldown_until` (e.g., a `Retry-After`-derived hold) even if the persisted `blocked_at + RATE_LIMITED_COOLDOWN_SECONDS` threshold has already elapsed.
- [x] Update the `query-caching` capability spec under `openspec/changes/recover-rate-limited-after-restart/specs/query-caching/spec.md` so the existing "Request-path selection uses cached usage snapshots" requirement gets the parallel rate-limit recovery scenarios.
- [x] Run `uv run pytest tests/unit/test_load_balancer.py -k rate_limited` and `uv run pytest tests/unit/test_load_balancer.py -k quota_exceeded` to confirm no regression in the QUOTA_EXCEEDED path.
- [x] Run `openspec validate --specs` and verify clean.
