- [x] Raise `max_decompressed_body_bytes` default in `app/core/config/settings.py` from `32 * 1024 * 1024` to `128 * 1024 * 1024`. Keep the `gt=0` validator and the field name (env var `CODEX_LB_MAX_DECOMPRESSED_BODY_BYTES` continues to work unchanged).
- [x] Add a `Large decompressed request body request_id=... path=... bytes=... max_bytes=...` warning log in `app/core/middleware/request_decompression.py` after successful decompression, gated on `decompressed_size * 5 >= max_size * 4` (integer arithmetic for the 80%-of-cap threshold). Import the standard `logging` module and `app.core.utils.request_id.get_request_id` alongside the existing settings/errors imports.
- [x] Add a regression `test_max_decompressed_body_bytes_default_leaves_room_for_slimmer` in `tests/unit/test_request_decompression_middleware.py` asserting `Settings().max_decompressed_body_bytes >= 128 * 1024 * 1024` so future tuning never drops below the slimmer's documented headroom.
- [x] Add `test_request_decompression_logs_warning_at_eighty_percent_of_cap` covering: cap = 256 bytes, payload that decompresses to >= 205 bytes, gzip encoding → status 200 AND exactly one `Large decompressed request body` warning record with the request path and `max_bytes=256` in the message.
- [x] Add `test_request_decompression_does_not_warn_below_eighty_percent` covering: cap = 4 KiB, tiny payload → status 200 AND no `Large decompressed request body` warning record.
- [x] Add an `ADDED Requirements` delta to `openspec/changes/raise-decompressed-body-cap/specs/proxy-admission-control/spec.md` codifying that the decompression cap is an OOM ceiling that MUST leave headroom for the responses slimmer, with scenarios covering the warn threshold, the 413 reject at the hard cap, and the slimmer-is-the-enforcer contract for `/backend-api/codex/responses` and `/v1/responses`.
- [x] Run `uv run pytest tests/unit/test_request_decompression_middleware.py -q` and confirm green. (11 passed)
- [x] Run `uv run pytest tests/integration/test_request_decompression.py -q` and confirm green. (2 passed; existing tests override the cap via env var and are unaffected by the default change.)
- [x] Run `uv run ruff check` (full repo) and `uv run ruff format --check` on the touched files. (All checks passed; reformat applied where needed.)
- [x] Run `uv run ty check app/core/middleware/request_decompression.py app/core/config/settings.py`. (All checks passed.)
- [ ] Run `openspec validate --specs` in an environment where the OpenSpec CLI is installed. (Skipped locally: tool not present on this machine and not part of repo CI; structural review only.)
