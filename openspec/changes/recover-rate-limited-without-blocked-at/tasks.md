- [x] Add a parallel no-blocked-at recovery branch in `_state_from_account` (`app/modules/proxy/load_balancer.py`) immediately after the existing `quota_exceeded` branch. The new branch fires when `account.status == AccountStatus.RATE_LIMITED`, `effective_blocked_at is None`, `effective_runtime_reset` is set and in the future, and the latest `primary_entry` is recent enough, reports `used_percent < 100`, and carries a `reset_at` strictly later than `effective_runtime_reset`.
- [x] Add regression `test_state_from_account_recovers_rate_limited_on_restart_without_blocked_at_when_usage_shows_new_reset_window` mirroring the QUOTA_EXCEEDED variant at line 454: `status=rate_limited`, `reset_at` future, `blocked_at` NULL, fresh primary with later reset_at and `used_percent < 100` → `state.status == AccountStatus.ACTIVE`.
- [x] Add regression `test_state_from_account_keeps_rate_limited_on_restart_when_fresh_primary_usage_is_missing_and_no_blocked_at` mirroring the QUOTA_EXCEEDED variant at line 480: usage `recorded_at` too old → `state.status == AccountStatus.RATE_LIMITED`.
- [x] Add regression `test_state_from_account_keeps_rate_limited_without_blocked_at_when_usage_stays_on_same_reset_window` mirroring the QUOTA_EXCEEDED variant at line 505: fresh primary but `reset_at` not strictly later than the stored runtime reset → `state.status == AccountStatus.RATE_LIMITED`.
- [x] Update `openspec/changes/recover-rate-limited-without-blocked-at/specs/query-caching/spec.md` so the existing `Request-path selection uses cached usage snapshots` requirement gets the parallel no-blocked-at scenarios.
- [x] Run `uv run pytest tests/unit/test_load_balancer.py -k "rate_limited or quota_exceeded"` and confirm no regression in the QUOTA_EXCEEDED path or the prior `recover-rate-limited-after-restart` scenarios.
